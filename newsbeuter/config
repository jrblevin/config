# Newsbeuter configuration
#
# Jason Blevins <jrblevin@sdf.lonestar.org>
# Chapel Hill, April 5, 2008

# Choose browser based on context (console, X, etc.).
browser sensible-browser

# Keep at most 1000 items per feed
max-items 1000

# On exit, remove items for feeds no longer subscribed to.
cleanup-on-quit yes

# Keybindings
bind-key  k           up
bind-key  j           down
bind-key  BACKSPACE   pageup
